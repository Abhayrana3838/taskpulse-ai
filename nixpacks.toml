[phases.build]
cmds = [
    "cd backend && pip install -r requirements.txt",
]

[phases.setup]
nixPkgs = ["python311", "gcc", "openssl", "postgresql", "libpq"]

[start]
cmd = "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
